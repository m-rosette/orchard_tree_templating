ekf_local:
  ros__parameters:
    frequency: 30.0
    sensor_timeout: 0.2
    two_d_mode: true
    reset_on_time_jump: true

    # Frames
    map_frame: map
    odom_frame: odom
    base_link_frame: amiga__base
    world_frame: odom

    publish_tf: true
    publish_acceleration: false

    # Wheel odometry
    odom0: /a200_0398/platform/odom/filtered
    odom0_queue_size: 10
    odom0_differential: false
    odom0_relative: false
    odom0_config: [
      true,  true,  false,   # x, y, z
      false, false, true,    # roll, pitch, yaw
      true,  true,  false,   # vx, vy, vz
      false, false, true,    # vroll, vpitch, vyaw
      false, false, false    # ax, ay, az
    ]

    # IMU
    imu0: /imu/data
    imu0_queue_size: 50
    imu0_differential: false
    imu0_relative: true
    imu0_remove_gravitational_acceleration: true
    imu0_config: [
      false, false, false,   # x, y, z
      false, false, true,    # roll, pitch, yaw
      false, false, false,   # vx, vy, vz
      false, false, true,    # vroll, vpitch, vyaw
      true,  true,  false    # ax, ay, az
    ]


# COMAND TO RUN:
# ros2 run robot_localization ekf_node \
#  --ros-args \
#  --params-file /home/marcus/apple_harvest_ws/src/orchard_tree_templating/tree_template/config/ekf_local.yaml \
#  -p use_sim_time:=true
